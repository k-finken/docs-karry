export const FeatureCallout = ({ name = 'this feature', color = '#16A34A' }) => (
  <div
    style={{
      border: `1px solid ${color}`,
      borderRadius: 12,
      padding: '16px 20px',
      background: 'rgba(22,163,74,0.06)',
      display: 'flex',
      gap: 12,
      alignItems: 'center',
    }}
  >
    <div
      style={{
        width: 36,
        height: 36,
        borderRadius: '50%',
        background: color,
        color: '#fff',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontWeight: 700,
        flexShrink: 0,
      }}
    >
      ✦
    </div>
    <div>
      <div style={{ fontWeight: 600, fontSize: 15 }}>Interactive demo</div>
      <div style={{ fontSize: 13, opacity: 0.8 }}>
        A rich, human-only preview of {name}.
      </div>
    </div>
  </div>
);
